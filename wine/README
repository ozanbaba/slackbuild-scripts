Wine is an Open Source implementation of the Windows API on top of X and Unix.

This requires fontforge and webcore-fonts.

you can tell the script to pause after configure to see if configure script worked
as expected. by default it won't pause. 

by default, on x86_64 systems, script will do paralel build with four jobs. 
